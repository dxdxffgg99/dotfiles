# INSTALL

```sh
ls ~/.config/kitty > /dev/null || mkdir ~/.config/kitty
curl -fsSL https://raw.githubusercontent.com/dxdxffgg99/dotfiles/main/.config/kitty/kitty.conf > ~/.config/kitty/kitty.conf
```
